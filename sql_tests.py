# sql_tests.py

# --- UPDATE ---
q1 = "UPDATE employees SET employee_name = 'hi'"

# --- INSERT ---
q2 = "INSERT INTO employees (employee_id, employee_name) VALUES (1, 12)"

# --- DELETE ---
q3 = "DELETE FROM employes"

# --- DROP ---
q4 = "DROP TABLE employes"

# --- TRUNCATE ---
q5 = "TRUNCATE employes"

# --- CONTROL (valid SELECT) ---
q6 = "SELECT employe FROM employees"

query = "SELECT location FROM employees"

query = "UPDATE employees SET employee_name = 1"
